# POLACARE Backend - Production Environment Variables
# Copy this to your deployment platform (Render, etc.)

# ============================================
# REQUIRED - App will not start without these
# ============================================

# Environment
NODE_ENV=production
PORT=5000
API_VERSION=v1

# Database (Supabase PostgreSQL)
# Get from: Supabase → Project Settings → Database → Connection string
DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres

# JWT Authentication
# Generate with: openssl rand -base64 48
JWT_SECRET=YOUR_64_CHARACTER_SECRET_HERE
JWT_EXPIRES_IN=7d

# CORS - Your Vercel frontend URL
CORS_ORIGIN=https://your-app.vercel.app

# ============================================
# RECOMMENDED - For production quality
# ============================================

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# OPTIONAL - Enable specific features
# ============================================

# Twilio (for real OTP SMS)
# Get from: twilio.com/console
# TWILIO_ACCOUNT_SID=AC...
# TWILIO_AUTH_TOKEN=...
# TWILIO_PHONE_NUMBER=+1...

# Google Gemini (for AI features)
# Get from: ai.google.dev
# GEMINI_API_KEY=...

# Supabase Storage (for file uploads)
# Get from: Supabase → Project Settings → API
# STORAGE_PROVIDER=supabase
# SUPABASE_URL=https://[PROJECT_REF].supabase.co
# SUPABASE_KEY=eyJ...

# AWS S3 (alternative file storage)
# STORAGE_PROVIDER=s3
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_S3_BUCKET=...
# AWS_S3_REGION=ap-southeast-1

# Email (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=...
# SMTP_PASS=...
# SMTP_FROM=noreply@polacare.com

# ============================================
# SECURITY NOTES
# ============================================
#
# 1. NEVER commit real secrets to git
# 2. Use Render's "Generate" for JWT_SECRET
# 3. Use Supabase connection pooler URL for production
# 4. Rotate JWT_SECRET periodically
# 5. Set CORS_ORIGIN to exact Vercel URL (no wildcards)
#

