# 🚀 Deploy Frontend บน Vercel (ง่ายมาก - ไม่ต้องใช้ Backend)

## 🎯 เป้าหมาย

Deploy Frontend พร้อมระบบสมัครสมาชิกที่ทำงานได้ด้วยตัวเอง (ไม่ต้องใช้ Backend)

---

## 📋 ขั้นตอน

### 1️⃣ เปลี่ยนเป็น Mock AuthContext

1. เปิด `App.tsx`
2. เปลี่ยน import จาก:
   ```typescript
   import { AuthProvider } from './contexts/AuthContext';
   ```
   เป็น:
   ```typescript
   import { AuthProvider } from './contexts/AuthContext.mock';
   ```

3. Save

---

### 2️⃣ Deploy บน Vercel

#### 2.1 สร้าง Vercel Account

1. ไปที่ [vercel.com](https://vercel.com)
2. Sign in with GitHub

#### 2.2 Import Project

1. คลิก **"Add New..."** → **"Project"**
2. เลือก repository: `weenahee04/polacare`
3. Vercel จะ detect อัตโนมัติว่าเป็น Vite project

#### 2.3 ตั้งค่า Build

| Setting | Value |
|---------|-------|
| **Framework Preset** | Vite |
| **Root Directory** | `.` (root) |
| **Build Command** | `npm run build` |
| **Output Directory** | `dist` |
| **Install Command** | `npm install` |

#### 2.4 Deploy!

1. คลิก **"Deploy"**
2. รอ build (~1-2 นาที)
3. ✅ **เสร็จแล้ว!** ได้ Frontend URL

---

## ✅ ทดสอบ

1. เปิด Frontend URL
2. คลิก **"สมัครสมาชิก"**
3. กรอกข้อมูล:
   - ชื่อ-นามสกุล
   - เบอร์โทรศัพท์
   - รหัสผ่าน
   - เพศ, วันเกิด, น้ำหนัก, ส่วนสูง
4. คลิก **"สมัครสมาชิก"**
5. ✅ **สำเร็จ!** ข้อมูลจะเก็บใน localStorage

---

## 🎯 ฟีเจอร์ที่ทำงานได้

- ✅ สมัครสมาชิก (เก็บใน localStorage)
- ✅ Login ด้วย OTP (mock OTP - ดูใน console)
- ✅ Login ด้วย password (ถ้ามี account แล้ว)
- ✅ ดู Profile
- ✅ Logout

---

## 📝 หมายเหตุ

- **ข้อมูลเก็บใน localStorage** (จะหายเมื่อ clear browser data)
- **OTP จะแสดงใน Console** (สำหรับ demo)
- **ไม่ต้องใช้ Backend** - ทำงานได้ด้วยตัวเอง

---

## 🔄 ถ้าต้องการ Backend จริง

เปลี่ยนกลับมาใช้:
```typescript
import { AuthProvider } from './contexts/AuthContext';
```

และตั้งค่า `VITE_API_URL` ใน Vercel

---

## ✅ Checklist

- [ ] เปลี่ยนเป็น `AuthContext.mock.tsx` แล้ว
- [ ] Commit และ push ขึ้น GitHub แล้ว
- [ ] Deploy บน Vercel แล้ว
- [ ] ทดสอบสมัครสมาชิกได้แล้ว

---

## 🎉 เสร็จแล้ว!

ตอนนี้ Frontend พร้อมใช้งานแล้ว!

**URL**: `https://polacare.vercel.app` (หรือ URL ที่ Vercel ให้)

